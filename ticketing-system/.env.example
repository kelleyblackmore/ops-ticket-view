VITE_API_PROXY_TARGET=http://localhost:3000

# Optional: if your backend expects /api removed, uncomment the rewrite in vite.config.js
# and keep your frontend requests as /api/... so they map correctly.
